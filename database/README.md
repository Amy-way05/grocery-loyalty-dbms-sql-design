# Database Files

The original SQLite database is not included by default because it contains customer-style records such as names, phone numbers, emails, and zip codes.

For public portfolio use, share:

- Schema SQL.
- ERD.
- Query files.
- Aggregate outputs.
- Synthetic sample data, if needed.

Only add `ITC6000_GRP_PRJT.db` to this folder if the data is confirmed synthetic or approved for public release.
